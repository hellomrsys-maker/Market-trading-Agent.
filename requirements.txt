# ============================================================
# OptionAlpha Agent — Requirements
# Polyglot: Python (high-level API) + bindings to C++/Rust/Julia
# Self-developed AI: no external LLM API dependencies
# ============================================================

# --- Alpaca Trading SDK ---
alpaca-py>=0.38.0

# --- Data & Numerics ---
numpy>=1.26.0
pandas>=2.2.0
scipy>=1.13.0
statsmodels>=0.14.0

# --- Self-Developed AI: Core ML Stack ---
torch>=2.3.0                # PyTorch — RL + Transformer backbone
torchvision>=0.18.0
torchaudio>=2.3.0

# RL Framework
stable-baselines3>=2.3.0    # PPO / DDPG / SAC agents
gymnasium>=0.29.0           # RL environment
shimmy>=1.3.0               # Gymnasium compatibility shims

# Transformer / Attention
transformers>=4.41.0        # HuggingFace transformers (local, no API)
accelerate>=0.30.0          # Multi-device training acceleration
einops>=0.8.0               # Elegant tensor operations

# Classical ML Ensemble
xgboost>=2.0.0
lightgbm>=4.3.0
scikit-learn>=1.5.0
catboost>=1.2.5

# GPU Kernel Compilation
triton>=2.3.0               # OpenAI Triton for custom CUDA kernels (falls back to CPU)

# ONNX Export & Runtime (cross-language model serving)
onnx>=1.16.0
onnxruntime>=1.18.0        # CPU runtime; use onnxruntime-gpu for NVIDIA

# --- Julia Integration (via JuliaCall) ---
juliacall>=0.9.21           # Python↔Julia bridge for options pricing / Monte Carlo

# --- Rust Extension (via PyO3/Maturin) ---
# The Rust data processor is built as a .pyd wheel; install via:
#   cd engine/rust && maturin develop --release
# maturin>=1.5.0            # (dev dependency, not runtime)

# --- Time-series & Feature Engineering ---
ta-lib>=0.4.32              # Technical analysis indicators (C backend)
pandas-ta>=0.3.14b          # Pure-Python TA library (fallback)
arch>=7.0.0                 # GARCH volatility modelling

# --- Options Pricing & Greeks ---
py_vollib>=1.0.2            # Black-Scholes, Greeks via C extensions
mibian>=0.1.3               # Pure-Python options pricer (offline fallback)

# --- MCP & Tool Protocol ---
mcp>=1.3.0                  # Model Context Protocol SDK

# --- Scheduling & Async ---
schedule>=1.2.2
apscheduler>=3.10.4
asyncio-mqtt>=0.16.2

# --- Configuration & Env ---
python-dotenv>=1.0.0
pydantic>=2.7.0
pydantic-settings>=2.3.0

# --- Persistence & Caching ---
redis>=5.0.0                # In-memory state store
sqlalchemy>=2.0.30          # Relational DB for trade history
alembic>=1.13.0             # DB migrations

# --- Monitoring & Dashboard ---
rich>=13.7.0                # Terminal UI
textual>=0.65.0             # TUI framework (interactive dashboard)
prometheus-client>=0.20.0   # Metrics export
psutil>=5.9.0               # System resource monitoring

# --- Web Dashboard Backend ---
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0

# --- Logging ---
loguru>=0.7.2
structlog>=24.2.0

# --- Testing ---
pytest>=8.2.0
pytest-asyncio>=0.23.0
pytest-mock>=3.14.0
httpx>=0.27.0               # For testing FastAPI routes

# --- Dev / Build Tools ---
black>=24.4.0
isort>=5.13.0
mypy>=1.10.0
